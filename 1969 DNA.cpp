#include <stdio.h>
#include <stdlib.h>

using namespace std;

int main()
{
	int N, M, *dna;
	scanf("%d %d", &N, &M);
	dna = (int*)malloc(sizeof(int)*N*M);

	for(int i=0; i<N; i++)
		for(int j=0; j<M; j++)
			scanf("%c",)

	return 0;
}